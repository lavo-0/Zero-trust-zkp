# Zero-trust-zkp
Zero Trust authentication mini-project using Schnorr ZKP for passwordless login. Built with microservices (Auth, Gateway, Resource API). Issues short-lived HMAC tokens, enforces per-request verification, device binding, and adaptive trust scoring. Includes attack demos: replay, expired token, stolen token, flooding.
